{
  "name": "obsidian-ai-host",
  "version": "0.2.2",
  "private": true,
  "license": "MIT",
  "author": "Helfas",
  "description": "Optional shared AI backend for Taskdown and FSRS Flashcards: provider keys in the system keychain, streaming chat, mentions and RAG, workspace sandbox, permission tiers, and auditable agent runs.",
  "scripts": {
    "build": "node esbuild.config.mjs --production",
    "verify": "node scripts/verify-bundle.cjs",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "esbuild": "^0.24.0",
    "obsidian": "^1.8.0",
    "typescript": "^5.6.0"
  }
}
